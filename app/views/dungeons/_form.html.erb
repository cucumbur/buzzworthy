<%= form_for(@dungeon) do |f| %>
  <% if @dungeon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dungeon.errors.count, "error") %> prohibited this dungeon from being saved:</h2>

      <ul>
      <% @dungeon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label "Event string (eventid:probability;)" %><br>
    <%= f.text_field :events %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
