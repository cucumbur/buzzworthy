<table class="pure-table" style="margin-bottom: 1em;">
	<tbody>
  	<tr class="pure-table-odd">
  		<td><h3><%= message.subject %></h3>
  		from <%= link_to User.find(message.from_id).name, User.find(message.from_id) %> on <%= message.created_at %></td>

  	</tr>
  	<tr class ="" >
    	<td><%= emojify(message.content) %></td>
  	</tr>
	</tbody>
</table>